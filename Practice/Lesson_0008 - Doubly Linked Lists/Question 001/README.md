# Insert and Delete Node from a Doubly Linked List

## In **Previous**, you wrote code for **inserting** and **deleting** values to and from a **singly linked list**.

Now, using the same question, write the following **insert functions**:
- an insert function that inserts a new student at the **beginning** of the doubly linked list
- an insert function that inserts a new student at the **end** of the doubly linked list
- an insert function that inserts a new student into a **sorted doubly linked list**

Next, using the same question, write the following **delete functions**:
- a delete function that deletes a student from the **beginning** of the doubly linked list
- a delete function that deletes a student from the **end** of the doubly linked list
- a delete function that deletes a student from a **sorted doubly linked list**

Using the same program, write the following **display functions**:
- a display function that displays the values in the doubly linked list in **forward order**  
  (from beginning to end)
- a display function that displays the values in the doubly linked list in **reverse order**  
  (from end to beginning)

---

## Example Menu List

```text
Menu List:
--------------------------
1. Add student ID in the front of the list
2. Add student ID at the end of the list
3. Sort current student list based on ID and display
4. Delete student ID from the front of the list
5. Delete student ID from the end of the list
6. Delete student ID based on the user input
7. Display student list
8. Display the reverse student list
9. Exit
