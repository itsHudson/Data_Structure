# Write a program that accepts an integer argument indicating the total number of subjects, and the test scores are stored in a dynamically allocated array. The program calls the following functions: 
  - calcAverage – this function should accept an array argument to calculate the average of the test scores. 
  - findLowest – this function should find and return the lowest of the test scores array passed to it.
  - Input validation: Do not accept a negative number.

```text
Enter total number of subjects: 4
Enter test scores:
70
0
65
4

Average score = 34.75
Lowest score = 0
