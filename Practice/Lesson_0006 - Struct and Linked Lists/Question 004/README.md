# Write a struct declaration for a linked list. Your structure should contain the following data members: an integer data member containing a student ID and a next pointer variable that points to the structure. In the main function, you will need to assign a value to the student ID, assign it next to NULL (or 0), and finally display the contents of the data members.  

```text
Student ID: 1001
Next address: 0
