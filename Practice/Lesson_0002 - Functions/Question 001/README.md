# Write a C++ program to add two integers. Make four different functions of add() (refer to Figure 2) to add integers and display sum in main() function. These function will / will not return any result back to the main() function. 

```text
