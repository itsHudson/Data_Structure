# Using the program that you wrote in question 5, write an insert function that will insert a new student at the end of the linked list. Then, write a display function to display the values in the linked list.  

# Program Output
```
1001 -> 1002 -> 1003 -> NULL
```

# Insertion at END in Linked List
```
When inserting: InsertEnd(head, 1001)
Memory: head → [1001] → NULL

Insert again: InsertEnd(head, 1002)
Memory: head → [1001] → [1002] → NULL

Insert again: InsertEnd(head, 1003)
Memory: head → [1001] → [1002] → [1003] → NULL


Traversal Process
- Display function moves node by node: HeadPointer → Node1 → Node2 → Node3 → NULL
